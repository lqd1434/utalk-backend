import * as React from 'react'
import { Pane } from 'evergreen-ui'
import styles from './index.module.scss'

const Header = () => {
	return (
		<Pane height={70} className={styles.header} backgroundColor={'#F3F9F9'} elevation={2} borderRadius={5}>
			333
		</Pane>
	)
}

export default Header
